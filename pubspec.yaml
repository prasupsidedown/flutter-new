name: flutter_application_1
description: MobiTravel
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  google_generative_ai: ^0.4.6
  http: ^1.6.0
  flutter_secure_storage: ^10.3.0
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
